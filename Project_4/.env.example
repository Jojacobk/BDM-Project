POSTGRES_DB=rico
POSTGRES_USER=rico
POSTGRES_PASSWORD=rico
POSTGRES_DSN=postgresql://rico:rico@postgres:5432/rico

MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=rico-raw
MINIO_URL=http://storage:9000

OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=qwen2.5:3b

AIRFLOW_UID=50000
AIRFLOW__WEBSERVER__SECRET_KEY=project-4-local-only
RICO_DEFAULT_LIMIT=5
GIT_SHA=

# Optional. Leave empty for local testing; missing Slack never fails the DAG.
SLACK_WEBHOOK_URL=
# Optional Airflow connection fallback if SLACK_WEBHOOK_URL is empty.
SLACK_WEBHOOK_CONN_ID=

# Bonus Slack Socket Mode agent. Required only when running the agent profile.
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
AIRFLOW_BASE_URL=http://airflow-webserver:8080
AIRFLOW_USERNAME=airflow
AIRFLOW_PASSWORD=airflow
AIRFLOW_DAG_ID=rico_production_pipeline
BACKFILL_MAX_LIMIT=50
